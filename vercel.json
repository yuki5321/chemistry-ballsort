{
  "version": 2,
  "builds": [
    {
      "src": "server/simple.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["shared/**", "dist/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/simple.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/server/simple.ts"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
